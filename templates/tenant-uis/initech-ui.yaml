apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.tenantUis.initech.name }}
  labels:
    app: {{ .Values.tenantUis.initech.name }}
    chart: {{ include "demodeck-single-version.chart" . }}
    release: {{ .Release.Name }}
    tenant: initech
spec:
  replicas: {{ .Values.tenantUis.initech.replicaCount }}
  selector:
    matchLabels:
      app: {{ .Values.tenantUis.initech.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.tenantUis.initech.name }}
        tenant: initech
    spec:
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
      - name: {{ .Values.tenantUis.initech.name }}
        image: "{{ .Values.global.registry }}/{{ .Values.tenantUis.initech.image }}:{{ .Values.tenantUis.initech.tag }}"
        imagePullPolicy: {{ .Values.global.imagePullPolicy }}
        ports:
        - containerPort: {{ .Values.tenantUis.initech.port }}
          name: http
        env:
        {{- range $key, $value := .Values.tenantUis.initech.env }}
        - name: {{ $key }}
          value: {{ $value | quote }}
        {{- end }}
        livenessProbe:
          httpGet:
            path: /
            port: {{ .Values.tenantUis.initech.port }}
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /
            port: {{ .Values.tenantUis.initech.port }}
          initialDelaySeconds: 5
          periodSeconds: 5
        resources:
          {{- toYaml .Values.tenantUis.initech.resources | nindent 10 }}
        securityContext:
          {{- toYaml .Values.securityContext | nindent 10 }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.tenantUis.initech.name }}-service
  labels:
    app: {{ .Values.tenantUis.initech.name }}
    chart: {{ include "demodeck-single-version.chart" . }}
    release: {{ .Release.Name }}
    tenant: initech
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: {{ .Values.tenantUis.initech.port }}
    protocol: TCP
    name: http
  selector:
    app: {{ .Values.tenantUis.initech.name }}